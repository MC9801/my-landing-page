/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  experimental: {
    // Next.js 15 puede tener configuraciones experimentales
  },
}

module.exports = nextConfig